{
  "name": "@telegram-evm-faucet-bot/queue",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "publish": "wrangler publish",
    "dev": "wrangler dev --port 8788 --inspector-port 9230",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "lt": "./node_modules/.bin/lt --port 8788",
    "clean": "rm -rf node_modules"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.5",
    "@cloudflare/workers-types": "^4.20240725.0",
    "localtunnel": "^2.0.2",
    "typescript": "^5.5.2",
    "vitest": "1.5.0",
    "wrangler": "^3.60.3"
  },
  "dependencies": {
    "@upstash/qstash": "^2.6.3",
    "@upstash/redis": "^1.33.0",
    "viem": "^2.18.5"
  }
}